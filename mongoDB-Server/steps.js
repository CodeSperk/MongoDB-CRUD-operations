/**
 * Install server side: 
    --> npm init -y
    --> "start" : "node index.js" (Script at package.json)
    --> npm i express cors mongodb
    --> index.js with basic express setup including cors & middleware
    --> node_modules in the .gitignore file
 */

/**
 * MongoDB Connection: (MongoDB Basic project at : ahsanugps@gmail.com )
    --> Create a database user & save user name and password
    --> Choose a connection Methods : 
        Drivers > 1.Node.js 3. connection string to index.js
    --> update  add userName & password in index.js.
    --> comment 'await client.close()'
    --> Quick start: 
        1. Update user name & password if needed
        2. Add Ip address in access list if needed
        3. Finish and close
    --> Database Access: you can add new user or edit. 
    --> Network Access: ADD IP ADDRESS > Allow access from anywhere
    --> Database : browser collection >
          * you can delete default database and collections 
          * not necessary: you can add custom database and collection 
    --> 
 */